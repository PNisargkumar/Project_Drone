<?xml version="1.0"?>
<launch>
	<!-- Run the EKF filter node -->
	<node pkg="robot_localization" type="ekf_localization_node" name="ekf_localization">
      <rosparam command="load" file="$(find odometry)/config/ekf_localization.yaml"/>
    </node>
</launch>